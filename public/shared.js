/* NovaAgencyTEC — shared content and accessible interactions. No external services are called. */
(() => {
  const D = window.NOVA;
  const esc = s => String(s).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const arrow = '<span aria-hidden="true">↗</span>';
  const icons = [
    '<path d="M5 5h5v5H5zM14 14h5v5h-5zM7.5 10v6.5H14M16.5 14V7.5H10"/>',
    '<rect x="3" y="4" width="18" height="15" rx="2"/><path d="M3 9h18M8 9v10M6 6.5h1m2 0h1"/>',
    '<path d="M4 3v17h17M8 15v-4m5 4V7m5 8V4"/>',
    '<path d="m12 2 2.8 7.2L22 12l-7.2 2.8L12 22l-2.8-7.2L2 12l7.2-2.8z"/>'
  ];
  const icon = (n=0) => `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">${icons[n%4]}</svg>`;
  const num = n => String(n+1).padStart(2,'0');
  const tag = text => `<span class="tag">${esc(text)}</span>`;
  const brand = `<a class="brand" href="#top" aria-label="NovaAgencyTEC, inicio"><img src="assets/nova-icon.png" width="38" height="38" alt=""><span>nova<span class="brand-light">agency</span><sup>TEC</sup></span></a>`;
  const btn = (text, href='#contacto', style='primary') => `<a class="button ${style}" href="${href}">${text}${arrow}</a>`;
  const heading = (id, label) => `<div class="section-heading reveal"><span class="eyebrow">${label}</span><h2>${esc(D.headings[id].title)}</h2><p>${esc(D.headings[id].subtitle)}</p></div>`;
  const metrics = item => `<div class="case-metrics">${item.metrics.map(m=>`<div><strong>${esc(m.value)}</strong><span>${esc(m.label)}</span></div>`).join('')}</div>`;
  const caseText = i => `<p>${esc(D.cases[i].text)}</p>${metrics(D.cases[i])}<div class="tags">${D.cases[i].stack.map(tag).join('')}</div>`;
  let visualSequence=0;
  const caseVisual = i => {
    const uid=++visualSequence;
    const names=['Nova Recover','Nova Analytics','Ruleta de premios','RIGHT Botines','Nova · IA','Import Calabrese'];
    let inner='';
    if(i===0)inner=`<div class="mail-preview"><span class="mini-label">RECUPERACIÓN AUTOMÁTICA</span><div class="mail-bag">${icon(0)}</div><h4>Tu carrito te espera.</h4><div class="fake-btn">Volver a mi compra ↗</div></div><div class="visual-notice">${icon(0)}<span>Email personalizado<strong>Listo para recuperar la venta</strong></span></div>`;
    if(i===1)inner=`<div class="dash-top"><span>Resumen de tu negocio</span><span>↗</span></div><div class="dash-numbers"><div><small>Registros trackeados</small><strong>+150M</strong></div><div><small>Productos</small><strong>520</strong></div></div><svg class="chart" viewBox="0 0 500 150" aria-label="Gráfico ilustrativo"><defs><linearGradient id="cg${uid}" x2="0" y2="1"><stop stop-color="#7c3aed" stop-opacity=".5"/><stop offset="1" stop-color="#7c3aed" stop-opacity="0"/></linearGradient></defs><path d="M0 130 40 114 75 122 115 82 155 94 198 58 233 72 280 32 320 47 360 17 405 33 450 8 500 0V150H0Z" fill="url(#cg${uid})"/><path class="chart-line" d="M0 130 40 114 75 122 115 82 155 94 198 58 233 72 280 32 320 47 360 17 405 33 450 8 500 0" stroke="#8b5cf6" fill="none" stroke-width="3"/></svg><div class="chart-labels"><span>Ventas</span><span>Stock</span><span>Clientes</span><span>Campañas</span></div>`;
    if(i===2)inner=`<div class="wheel-wrap"><div class="wheel"><span>10%</span><span>PREMIO</span><span>15%</span><span>VOLVÉ</span></div><div class="wheel-center">N</div><span class="wheel-pointer">▼</span></div><div class="visual-notice">130+ giros en un mes <strong>Sin publicidad</strong></div>`;
    if(i===3)return `<div class="case-visual real-project visual-3"><div class="window-bar"><span class="window-dots">● ● ●</span><span>RIGHT Botines</span><span>↗</span></div><div class="real-project-image"><img src="assets/right-boot.webp" alt="Fotografía de botines del catálogo público de RIGHT" width="640" height="512" loading="lazy"></div><a class="real-source" href="https://right.com.ar" target="_blank" rel="noopener">Imagen de la web del cliente ↗</a></div>`;
    if(i===4)inner=`<div class="inbox-title">${icon(3)}<strong>Tu bandeja, en orden.</strong></div>${['Consulta por un pedido','Necesito cambiar mi talle','¡Me encantó mi compra!'].map((x,j)=>`<div class="inbox-row"><span class="avatar">${['M','A','L'][j]}</span><span>${x}<small>Clasificado automáticamente</small></span><em>${['Consulta','Reclamo','Positivo'][j]}</em></div>`).join('')}<div class="ai-response">✦ Respuesta sugerida por IA</div>`;
    if(i===5)return `<div class="case-visual real-project visual-5"><div class="window-bar"><span class="window-dots">● ● ●</span><span>importcalabrese.com</span><span>↗</span></div><div class="real-project-image"><img src="assets/calabrese-store.webp" alt="Imagen de la tienda física de Import Calabrese, Santa Fe, publicada en su web" width="1400" height="621" loading="lazy"></div><a class="real-source" href="https://importcalabrese.com" target="_blank" rel="noopener">Imagen de la web del cliente ↗</a></div>`;
    return `<div class="case-visual visual-${i}" aria-label="Visualización conceptual de ${names[i]}"><div class="window-bar"><span class="window-dots">● ● ●</span><span>${names[i]}</span><span>↗</span></div><div class="visual-body">${inner}</div><span class="concept-label">Vista conceptual · proyecto real</span></div>`;
  };
  function header(version) {
    return `<a class="skip" href="#contenido">Saltar al contenido</a><div class="reading-progress" aria-hidden="true"></div><header class="site-header">${brand}<nav class="main-nav" aria-label="Navegación principal"><a href="#servicios">Servicios</a><a href="#casos">Casos de éxito</a><a href="#ecosistema">Ecosistema</a><a href="#integraciones">Integraciones</a></nav><div class="nav-actions">${btn('Hablemos','#contacto','small')}<button class="menu-toggle icon-button" aria-label="Abrir menú" aria-expanded="false">☰</button></div></header><div class="mobile-nav" hidden><a href="#servicios">Servicios</a><a href="#casos">Casos de éxito</a><a href="#ecosistema">Ecosistema</a><a href="#proceso">Proceso</a><a href="#integraciones">Integraciones</a><a href="#contacto">Contacto</a></div>`;
  }
  function stats() {return `<section class="stats-strip" aria-label="Resultados de proyectos">${D.stats.map((s,i)=>`<div class="stat reveal"><span class="mini-label">RESULTADO / ${num(i)}</span><strong>${esc(s.value)}</strong><p>${esc(s.label)}</p></div>`).join('')}</section>`;}
  function industries() {return `<section id="industrias" class="section industries-section">${heading('industrias','PARA QUIÉN TRABAJAMOS')}<div class="industries-layout"><div class="industry-buttons" role="tablist" aria-label="Elegí tu industria">${D.industries.map((x,i)=>`<button id="industry-tab-${i}" role="tab" aria-controls="industry-panel" aria-selected="${i===0}" tabindex="${i===0?0:-1}" data-industry="${i}">${esc(x.title)}<span>↗</span></button>`).join('')}</div><div class="industry-panel" id="industry-panel" role="tabpanel" aria-labelledby="industry-tab-0"><span class="industry-art" aria-hidden="true">${icon(0)}</span><span class="eyebrow">TECNOLOGÍA PARA TU NEGOCIO</span><h3>${esc(D.industries[0].title)}</h3><p>${esc(D.industries[0].text)}</p>${btn('Hablemos de tu proyecto','#contacto','text-button')}</div></div></section>`;}
  function process() {return `<section id="proceso" class="section process-section">${heading('proceso','DE LA IDEA A LA REALIDAD')}<div class="process-grid">${D.process.map((x,i)=>`<article class="process-step reveal"><span class="process-number">${num(i)}</span><div class="process-line"></div><h3>${esc(x.title)}</h3><p>${esc(x.text)}</p></article>`).join('')}</div></section>`;}
  function integrations() {return `<section id="integraciones" class="section integrations-section">${heading('integraciones','TODO CONECTADO')}<div class="integration-categories">${D.integrations.map((c,i)=>`<article class="integration-group reveal"><h3>${icon(i)}${esc(c.title)}</h3><div class="tags">${c.items.map(tag).join('')}</div></article>`).join('')}</div><div class="integration-extra"><span>También integramos</span>${['Meta / Facebook Ads','Twilio SMS','Node.js / Python'].map(tag).join('')}</div><div id="tecnologias" class="tech-section"><div><span class="eyebrow">STACK TECNOLÓGICO</span><h3>Tecnologías que usamos</h3><p>${esc(D.headings.tecnologias.subtitle)}</p></div><div class="tech-list">${D.technologies.map(x=>`<div><strong>${esc(x.title)}</strong><span>${esc(x.text)}</span></div>`).join('')}</div></div></section>`;}
  function testimonials() {return `<section id="testimonios" class="section testimonials-section">${heading('testimonios','LO DICEN NUESTROS CLIENTES')}<div class="quotes-grid">${D.testimonials.map((x,i)=>`<figure class="quote-card reveal"><div class="quote-top"><span class="quote-mark">“</span><span aria-label="5 de 5 estrellas">★★★★★</span></div><blockquote>${esc(x.quote)}</blockquote><figcaption><span class="avatar">${['E','R','IC'][i]}</span><span><strong>${esc(x.name)}</strong><small>${esc(x.role)}</small></span></figcaption></figure>`).join('')}</div></section>`;}
  function faq() {return `<section id="faq" class="section faq-section">${heading('faq','PREGUNTAS FRECUENTES')}<div class="faq-list">${D.faqs.map((x,i)=>`<details class="faq-item reveal"><summary><span class="faq-num">${num(i)}</span>${esc(x.question)}<span class="faq-plus" aria-hidden="true">+</span></summary><div><p>${esc(x.answer)}</p></div></details>`).join('')}</div></section>`;}
  function contact() {return `<section id="contacto" class="section contact-section"><div class="contact-copy reveal"><span class="eyebrow"><i class="status-dot"></i> HABLEMOS DE LO QUE VIENE</span><h2>¿Listo para<br><em>automatizar</em><br>o rediseñar<br>tu negocio?</h2><p>Contanos de qué se trata tu proyecto. Te respondemos en menos de 24 horas con un diagnóstico inicial gratuito.</p><div class="contact-links"><a href="https://wa.me/5493424633285" target="_blank" rel="noopener">WhatsApp <span>Respuesta en minutos ↗</span></a><a href="mailto:novaagencytec@gmail.com">Email <span>novaagencytec@gmail.com ↗</span></a><a href="https://instagram.com/novaagencytec" target="_blank" rel="noopener">Instagram <span>@novaagencytec ↗</span></a><button class="share-button">Compartir <span>Mandale esta web a alguien ↗</span></button></div><p class="share-status" role="status"></p></div><form class="contact-form reveal"><span class="mini-label">EL PRIMER PASO ES UNA CHARLA</span><h3>Contanos tu proyecto.</h3><p>Sin tecnicismos, sin compromiso. Solo contanos qué necesitás.</p><label>Tu nombre<input name="name" autocomplete="name" placeholder="Ej: Martín" required maxlength="100"></label><label>Tu negocio<input name="business" autocomplete="organization" placeholder="Ej: Mi tienda online" required maxlength="150"></label><label>¿Qué necesitás?<select name="service" required><option value="">Seleccioná una opción</option>${['Automatización de procesos','Diseño web / Personalización TiendaNube','Dashboard / panel de control','Integración con IA','App o sistema a medida','No sé, necesito asesoramiento'].map(x=>`<option>${esc(x)}</option>`).join('')}</select></label><label>Contanos más<textarea name="message" placeholder="Qué te gustaría mejorar, qué te lleva tiempo…" rows="3" maxlength="2000"></textarea></label><button class="button primary" type="submit">Preparar mensaje de WhatsApp ${arrow}</button><small>Se abrirá WhatsApp con tu mensaje listo para enviar.</small></form></section>`;}
  function footer() {return `<footer class="site-footer">${brand}<p>Automatización de procesos, diseño web, personalización TiendaNube y dashboards con IA para ecommerce y negocios digitales en Argentina.</p><div class="footer-links"><a href="#servicios">Servicios</a><a href="#casos">Casos de éxito</a><a href="#proceso">Proceso</a><a href="#tecnologias">Tecnologías</a><a href="#contacto">Contacto</a><a href="#top">Volver arriba ↑</a></div><div class="footer-bottom"><span>© ${new Date().getFullYear()} NovaAgencyTEC</span><span>Hecho en Argentina. Pensado para crecer.</span></div></footer>`;}
  function apps(mode='grid') {return `<section id="ecosistema" class="section apps-section">${heading('ecosistema','EL UNIVERSO NOVA')}<div class="apps-${mode}">${D.apps.map((x,i)=>`<article class="app-card reveal"><div class="app-card-top"><span class="app-symbol">${icon(i)}</span><span class="live-tag"><i class="status-dot"></i> Live</span></div><span class="mini-label">NOVA / ${num(i)}</span><h3>${esc(x.title)}</h3><p>${esc(x.text)}</p><div class="tags">${x.stack.map(tag).join('')}</div></article>`).join('')}</div></section>`;}
  function cases(mode='grid') {return `<section id="casos" class="section cases-section">${heading('casos','PROYECTOS REALES. IMPACTO REAL.')}<div class="cases-${mode}">${D.cases.map((x,i)=>`<article class="case-card reveal">${caseVisual(i)}<div class="case-copy"><span class="eyebrow">${esc(x.category)}</span><h3>${esc(x.title)}</h3>${caseText(i)}</div></article>`).join('')}</div></section>`;}
  function services(mode='grid') {return `<section id="servicios" class="section services-section">${heading('servicios','DISEÑAMOS. CONECTAMOS. AUTOMATIZAMOS.')}<div class="services-${mode}">${D.services.map((x,i)=>`<article class="service-card reveal"><div class="service-top"><span class="service-icon">${icon(i)}</span><span class="mini-label">${num(i)} / 04</span></div><h3>${esc(x.title)}</h3><p>${esc(x.text)}</p><a href="#contacto" class="service-link" aria-label="Consultar por ${esc(x.title)}">Hagámoslo realidad ${arrow}</a></article>`).join('')}</div></section>`;}
  function marquee() {const text='<span>Automatización</span><b>✳</b><span>Diseño web</span><b>✳</b><span>Inteligencia artificial</span><b>✳</b><span>Dashboards</span><b>✳</b>';return `<div class="marquee" aria-label="Automatización, Diseño web, Inteligencia artificial, Dashboards"><div aria-hidden="true">${text}${text}</div></div>`;}

  let paused=matchMedia('(prefers-reduced-motion: reduce)').matches;
  try { if(localStorage.getItem('nova-motion')) paused=localStorage.getItem('nova-motion')==='paused'; } catch{}
  const motionListeners=[];
  function applyMotion() {document.body.classList.toggle('motion-paused',paused);document.querySelectorAll('.motion-toggle').forEach(b=>{b.textContent=paused?'▷':'Ⅱ';b.setAttribute('aria-label',paused?'Activar animaciones':'Pausar animaciones');b.title=paused?'Activar animaciones':'Pausar animaciones';});motionListeners.forEach(fn=>fn(paused));}
  function setup() {
    applyMotion();
    document.querySelectorAll('.motion-toggle').forEach(b=>b.addEventListener('click',()=>{paused=!paused;try{localStorage.setItem('nova-motion',paused?'paused':'active');}catch{}applyMotion();}));
    const menu=document.querySelector('.menu-toggle'), mobile=document.querySelector('.mobile-nav');
    menu?.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')!=='true';menu.setAttribute('aria-expanded',open);menu.setAttribute('aria-label',open?'Cerrar menú':'Abrir menú');menu.textContent=open?'×':'☰';mobile.hidden=!open;});
    mobile?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{mobile.hidden=true;menu.setAttribute('aria-expanded','false');menu.textContent='☰';menu.setAttribute('aria-label','Abrir menú');}));
    document.addEventListener('keydown',e=>{if(e.key==='Escape' && mobile && !mobile.hidden){menu.click();menu.focus();}});
    const observer=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target);}});},{threshold:.08});
    document.querySelectorAll('.reveal').forEach((el,i)=>{el.style.setProperty('--delay',`${i%3*65}ms`);observer.observe(el);});
    let scrollFrame=false;
    const onScroll=()=>{if(scrollFrame)return;scrollFrame=true;requestAnimationFrame(()=>{const y=scrollY,h=document.documentElement.scrollHeight-innerHeight;document.querySelector('.reading-progress')?.style.setProperty('transform',`scaleX(${h?y/h:0})`);document.querySelector('.site-header')?.classList.toggle('scrolled',y>40);document.querySelectorAll('[data-parallax]').forEach(el=>{if(!paused && innerWidth>760)el.style.transform=`translateY(${Math.min(y,1200)*Number(el.dataset.parallax)}px)`;});scrollFrame=false;});};
    addEventListener('scroll',onScroll,{passive:true});onScroll();
    document.querySelectorAll('[data-tilt]').forEach(el=>{el.addEventListener('pointermove',e=>{if(paused||e.pointerType==='touch')return;const r=el.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;el.style.setProperty('--rx',`${-y*7}deg`);el.style.setProperty('--ry',`${x*9}deg`);el.style.setProperty('--mx',`${(x+.5)*100}%`);el.style.setProperty('--my',`${(y+.5)*100}%`);});el.addEventListener('pointerleave',()=>{el.style.setProperty('--rx','0deg');el.style.setProperty('--ry','0deg');});});
    const tabs=[...document.querySelectorAll('[data-industry]')];
    tabs.forEach((b,i)=>{b.addEventListener('click',()=>{tabs.forEach(x=>{x.setAttribute('aria-selected',x===b);x.tabIndex=x===b?0:-1;});const x=D.industries[i],p=document.querySelector('.industry-panel');p.setAttribute('aria-labelledby',b.id);p.querySelector('h3').textContent=x.title;p.querySelector('p').textContent=x.text;p.querySelector('.industry-art').innerHTML=icon(i);});b.addEventListener('keydown',e=>{let n=i;if(e.key==='ArrowRight'||e.key==='ArrowDown')n=(i+1)%tabs.length;else if(e.key==='ArrowLeft'||e.key==='ArrowUp')n=(i+tabs.length-1)%tabs.length;else if(e.key==='Home')n=0;else if(e.key==='End')n=tabs.length-1;else return;e.preventDefault();tabs[n].click();tabs[n].focus();});});
    document.querySelector('.contact-form')?.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(e.currentTarget);const message=`Hola Nova! Soy ${data.get('name')}. Mi negocio es ${data.get('business')}.\nNecesito: ${data.get('service')}.\n${data.get('message')||''}`;window.open(`https://wa.me/5493424633285?text=${encodeURIComponent(message)}`,'_blank','noopener,noreferrer');});
    document.querySelector('.share-button')?.addEventListener('click',async()=>{const status=document.querySelector('.share-status');try{await navigator.clipboard.writeText('https://novaagency.info');status.textContent='Enlace de Nova copiado.';}catch{status.textContent='Compartí este enlace: https://novaagency.info';}});
  }
  window.NovaUI={D,esc,icon,num,tag,arrow,brand,btn,heading,metrics,caseText,caseVisual,header,stats,industries,process,integrations,testimonials,faq,contact,footer,apps,cases,services,marquee,setup,isPaused:()=>paused,onMotion:fn=>motionListeners.push(fn)};
})();
