(() => {
 const N=NovaUI,{D,esc,icon,btn}=N;
 const imageVisual=i=>i===3?`<div class="photo-project"><img src="assets/right-boot.webp" width="640" height="512" alt="Fotografía de botines publicada por RIGHT" loading="lazy"><div class="photo-project-caption"><img src="assets/right-logo.webp" width="480" height="192" alt="RIGHT"><span>PROYECTO REAL / E-COMMERCE</span></div></div>`:i===5?`<div class="photo-project photo-store"><img src="assets/calabrese-store.webp" width="1400" height="621" alt="Local de Import Calabrese en Santa Fe" loading="lazy"><div class="photo-project-caption"><strong>IMPORT<br>CALABRESE.</strong><span>PROYECTO REAL / DISEÑO WEB</span></div></div>`:N.caseVisual(i);
 const order=[3,1,5,0,2,4];
 const gallery=()=>`<div class="dimension-gallery" aria-roledescription="carrusel" aria-label="Proyectos de Nova"><div class="gallery-stage"><div class="gallery-floor" aria-hidden="true"></div>${order.map((i,j)=>`<article class="gallery-slide ${j===0?'is-center':j===1?'is-right':j===5?'is-left':'is-back'}" data-slide="${j}" ${j>1&&j<5?'aria-hidden="true"':''}><button data-gallery-select="${j}" tabindex="${j===0?0:-1}" aria-label="Seleccionar proyecto ${esc(D.cases[i].title)}">${imageVisual(i)}<div class="gallery-card-label"><span>${esc(D.cases[i].category)}</span><strong>${['RIGHT Botines','Nova Analytics','Import Calabrese','Nova Recover','Ruleta de premios','Agente de emails'][j]}</strong><b>↗</b></div></button></article>`).join('')}</div><div class="gallery-controls"><button class="gallery-prev icon-button" aria-label="Proyecto anterior">←</button><div><span class="gallery-counter" aria-live="polite">01 / 06</span><button class="gallery-open" data-open-case="3">Explorar este proyecto ↗</button></div><button class="gallery-next icon-button" aria-label="Proyecto siguiente">→</button></div></div>`;
 const services=()=>`<section id="servicios" class="section dimension-services">${N.heading('servicios','UN NUEVO NIVEL PARA TU NEGOCIO')}<div class="dimension-service-stack">${D.services.map((x,i)=>`<article class="dimension-service" style="--card-index:${i};--service-color:${['#7c3aed','#f97316','#2563eb','#06b6d4'][i]}"><div class="dimension-service-info"><div class="dimension-service-top"><span>${N.num(i)} / 04</span>${icon(i)}</div><h3>${esc(x.title)}</h3><p>${esc(x.text)}</p>${btn('Hagámoslo realidad','#contacto','text-button')}</div><div class="dimension-service-art">${imageVisual([0,5,1,4][i])}<span class="service-art-word" aria-hidden="true">${['Conectar.','Diseñar.','Entender.','Crear.'][i]}</span></div></article>`).join('')}</div></section>`;
 const cases=()=>`<section id="casos" class="section dimension-cases">${N.heading('casos','EL IMPACTO SE PUEDE VER')}<div class="case-filters" aria-label="Filtrar proyectos">${['Todos','Automatización','Diseño web','IA & Datos'].map((x,i)=>`<button data-filter="${i}" aria-pressed="${i===0}">${x}<span>${[6,3,1,2][i]}</span></button>`).join('')}</div><div class="dimension-case-grid">${D.cases.map((x,i)=>`<article class="dimension-case reveal" data-case-card="${i}" data-category="${[1,3,1,1,3,2][i]}"><button class="case-open-button" data-open-case="${i}" aria-label="Leer caso: ${esc(x.title)}"><div class="dimension-case-image">${imageVisual(i)}<span class="case-open-circle">↗</span></div><div class="dimension-case-label"><span class="eyebrow">${esc(x.category)}</span><h3>${esc(x.title)}</h3><span class="dimension-case-summary"><strong>${esc(x.metrics[0].value)}</strong>${esc(x.metrics[0].label)}</span></div></button></article>`).join('')}</div><p class="filter-status" aria-live="polite">6 proyectos</p></section>`;
 const apps=()=>`<section id="ecosistema" class="section dimension-apps">${N.heading('ecosistema','SEIS HERRAMIENTAS. UN MISMO UNIVERSO.')}<div class="constellation-layout"><div class="constellation" aria-label="Mapa del ecosistema Nova"><div class="constellation-orbit" aria-hidden="true"></div><div class="constellation-core" aria-hidden="true">N<span>NOVA</span></div><div class="constellation-lines" aria-hidden="true">${D.apps.map((_,i)=>`<span style="--angle:${i*60}deg"></span>`).join('')}</div>${D.apps.map((x,i)=>`<button class="constellation-node node-${i} ${i===0?'active':''}" data-constellation="${i}" aria-pressed="${i===0}" aria-controls="constellation-panel">${icon(i)}<span>${esc(x.title.replace('Nova ',''))}</span></button>`).join('')}</div><div class="constellation-panel" id="constellation-panel"><span class="live-tag"><i class="status-dot"></i> Live</span><span class="eyebrow">EL ECOSISTEMA NOVA</span><h3>${esc(D.apps[0].title)}</h3><p>${esc(D.apps[0].text)}</p><div class="tags">${D.apps[0].stack.map(N.tag).join('')}</div>${btn('Conocé más','#contacto','text-button')}</div></div></section>`;
 const dialogs=()=>D.cases.map((x,i)=>`<dialog class="project-dialog" id="case-dialog-${i}" aria-labelledby="case-dialog-title-${i}"><button class="dialog-close icon-button" aria-label="Cerrar caso">×</button><div class="dialog-layout"><div class="dialog-art">${imageVisual(i)}</div><div class="dialog-copy"><span class="eyebrow">${esc(x.category)}</span><h2 id="case-dialog-title-${i}">${esc(x.title)}</h2>${N.caseText(i)}${btn('Quiero algo así','#contacto','primary')}</div></div></dialog>`).join('');
 document.querySelector('#app').innerHTML=N.header(4)+`<main id="contenido"><section class="dimension-hero" id="top"><div class="dimension-ambient" aria-hidden="true"></div><span class="eyebrow">AUTOMATIZACIÓN · DISEÑO WEB · IA · DASHBOARDS</span><h1><span>Automatizamos tu negocio.</span>Vos solo <em>vendés.</em></h1><p>${esc(D.hero)}</p><div class="dimension-hero-ctas">${btn('Empecemos a crear','#contacto')}${btn('Explorá nuestros casos','#casos','secondary')}</div>${gallery()}<span class="dimension-hero-note">DISEÑO, TECNOLOGÍA Y RESULTADOS QUE SE CONECTAN.</span></section>${N.stats()}${services()}${N.marquee()}${cases()}${apps()}${N.industries()}${N.process()}${N.integrations()}${N.testimonials()}${N.faq()}${N.contact()}</main>${N.footer()}${dialogs()}`;
 document.body.classList.add('js-ready');N.setup();
 let current=0;
 function showSlide(i){current=(i+6)%6;document.querySelectorAll('[data-slide]').forEach((el,j)=>{const delta=(j-current+6)%6;el.classList.remove('is-center','is-left','is-right','is-back');el.classList.add(delta===0?'is-center':delta===1?'is-right':delta===5?'is-left':'is-back');el.setAttribute('aria-hidden',delta>1&&delta<5?'true':'false');el.querySelector('button').tabIndex=delta===0?0:-1;});document.querySelector('.gallery-counter').textContent=`${N.num(current)} / 06`;document.querySelector('.gallery-open').dataset.openCase=order[current];}
 document.querySelector('.gallery-prev').addEventListener('click',()=>showSlide(current-1));document.querySelector('.gallery-next').addEventListener('click',()=>showSlide(current+1));document.querySelectorAll('[data-gallery-select]').forEach(b=>b.addEventListener('click',()=>{const i=Number(b.dataset.gallerySelect);if(i===current)openCase(order[i],b);else showSlide(i);}));
 document.querySelector('.dimension-gallery').addEventListener('keydown',e=>{if(e.key==='ArrowLeft'){e.preventDefault();showSlide(current-1);}if(e.key==='ArrowRight'){e.preventDefault();showSlide(current+1);}});
 let touchStart=null;document.querySelector('.gallery-stage').addEventListener('touchstart',e=>{touchStart={x:e.touches[0].clientX,y:e.touches[0].clientY};},{passive:true});document.querySelector('.gallery-stage').addEventListener('touchend',e=>{if(!touchStart)return;const dx=e.changedTouches[0].clientX-touchStart.x,dy=e.changedTouches[0].clientY-touchStart.y;if(Math.abs(dx)>60&&Math.abs(dx)>Math.abs(dy))showSlide(current+(dx<0?1:-1));touchStart=null;},{passive:true});
 let returnFocus=null;
 function openCase(i,trigger){returnFocus=trigger;document.querySelector(`#case-dialog-${i}`).showModal();document.body.classList.add('dialog-open');}
 document.querySelectorAll('[data-open-case]').forEach(b=>b.addEventListener('click',()=>openCase(Number(b.dataset.openCase),b)));
 document.querySelectorAll('.project-dialog').forEach(d=>{d.querySelector('.dialog-close').addEventListener('click',()=>d.close());d.addEventListener('click',e=>{if(e.target===d){const r=d.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)d.close();}});d.addEventListener('close',()=>{document.body.classList.remove('dialog-open');returnFocus?.focus({preventScroll:true});});d.querySelector('a[href="#contacto"]').addEventListener('click',()=>{d.close();document.querySelector('#contacto').scrollIntoView({behavior:N.isPaused()?'instant':'smooth'});});});
 document.querySelectorAll('[data-filter]').forEach(b=>b.addEventListener('click',()=>{const filter=Number(b.dataset.filter);document.querySelectorAll('[data-filter]').forEach(x=>x.setAttribute('aria-pressed',x===b));let n=0;document.querySelectorAll('[data-case-card]').forEach(c=>{const show=!filter||Number(c.dataset.category)===filter;c.hidden=!show;if(show){c.classList.add('visible');n++;}});document.querySelector('.filter-status').textContent=`${n} ${n===1?'proyecto':'proyectos'}`;}));
 document.querySelectorAll('[data-constellation]').forEach((b,i)=>b.addEventListener('click',()=>{document.querySelectorAll('[data-constellation]').forEach(x=>{x.classList.toggle('active',x===b);x.setAttribute('aria-pressed',x===b);});const x=D.apps[i],p=document.querySelector('.constellation-panel');p.querySelector('h3').textContent=x.title;p.querySelector('p').textContent=x.text;p.querySelector('.tags').innerHTML=x.stack.map(N.tag).join('');}));
})();
